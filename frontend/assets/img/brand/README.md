# 惑梦银白猫耳视觉资源

本目录中的图形以 `D:\网站\log\参考\银白猫耳角色-三视图-v1.png` 为角色设定依据，风格参考来自 `D:\网站\log\参照`。所有图形均不含文字。

- `app-icon-1024.png`：网站与 Android 应用图标母版。
- `launch-loading-1080x1920.png`：无本地会话快照时的加载兜底插画。
- `default-avatar-512.png`：账号和角色资源缺失时的默认头像。
- `empty-state-512.png`：列表或会话为空时的状态插画。
- `network-error-512.png`：网络或对话服务不可用时的状态插画。
- `pull-refresh-*.png`：下拉刷新准备、执行、完成三态图形。

页面引用应携带 `v=20260901-persistent-pages`，避免浏览器继续使用旧品牌缓存。
