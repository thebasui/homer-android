<!--
Copyright (C) 2024–2026 Aiko Hanasaki
SPDX-License-Identifier: AGPL-3.0-only
-->

# 📕 Memory Books (SillyTavern 拡張機能)

自動的かつ構造化された信頼性の高いメモリ作成を行うための、次世代 SillyTavern 拡張機能です。チャット内のシーンをマークし、AI を使って JSON ベースの要約を生成し、それらをロアブックのエントリとして保存します。グループチャット、高度なプロファイル管理、サイドプロンプト/トラッカー、および多段階のメモリ統合をサポートしています。

### ❓ 用語集
- Scene（シーン）→ Memory（メモリ）  
- One saved fact（保存した単一の事実）→ Clip（クリップ）  
- Ongoing tracker（継続更新されるトラッカー）→ Side Prompt（サイドプロンプト）  
- Many Memories（複数のメモリ）→ Summary / Consolidation（要約 / 統合）  
- One long entry（長くなった1つのエントリ）→ Compaction（圧縮）

### クリップ vs サイドプロンプト

<details>
<summary><strong>クリップ vs サイドプロンプト</strong></summary>

| **クリップ** | **サイドプロンプト** |
|---|---|
| 選択したチャットテキストをメモリーブックのエントリに保存します。 | AI にチャットを確認させ、トラッカーエントリを更新します。 |
| 1つの明確な事実、台詞、約束、好み、アイテム、メモに向いています。 | 時間とともに変化する情報に向いています。 |
| 考え方: 「このメモをピン留めする」。 | 考え方: 「このセクションを更新し続ける」。 |

</details>

詳しい説明は [ユーザーガイド](USER_GUIDE-JP.md#️-クリップ-vs-サイドプロンプト) を参照してください。

### 圧縮 vs 統合

<details>
<summary><strong>圧縮 vs 統合</strong></summary>

| **圧縮** | **統合** |
|---|---|
| STMB 管理下の既存エントリ1件を短くします。 | 複数のメモリや要約を、より上位の要約1件にまとめます。 |
| Clip、Side Prompt、Memory エントリが有用だが長くなりすぎたときに使います。 | 複数のメモリを Arc、Chapter、Book などの大きな要約にまとめる準備ができたときに使います。 |
| 考え方: 「この1件を刈り込む」。 | 考え方: 「これらのメモリをまとめて要約にする」。 |

</details>

詳しい説明は [ユーザーガイド](USER_GUIDE-JP.md#-圧縮-vs-統合) を参照してください。

## ❗ 最初にお読みください！

まずはここから：
* ⚠️‼️ インストールに関する注意点（特に Text Completion API を使用している場合）については、[必須要件](#-必須要件-prerequisites)をお読みください。
* 📽️ [クイックスタート動画](https://youtu.be/mG2eRH_EhHs) - 英語のみです（すみません、私が最も得意な言語です）。
* ❓ [よくある質問 (FAQ)](#faq)
* 🛠️ [トラブルシューティング](#troubleshooting)

その他のリンク：
* 📘 [ユーザーガイド (日本語)](USER_GUIDE-JP.md)
* 💡 [STMB の仕組み (日本語)](howSTMBworks-ja.md)
* 📋 [バージョン履歴 & 更新ログ](../changelog.md)
* 💡 [📕 Memory Books と 📚 Lorebook Ordering の併用について](https://github.com/aikohanasaki/SillyTavern-LorebookOrdering/blob/main/guides/STMB%20and%20STLO%20-%20Japanese.md)

> 注意：さまざまな言語をサポートしています。対応リストについては [`/locales`](../locales) フォルダを参照してください。国際化/ローカライズされた README とユーザーガイドは [`/userguides`](./) フォルダにあります。
> ロアブックコンバーターとサイドプロンプトテンプレートライブラリは [`/resources`](../resources) フォルダにあります。

## 📑 目次

- [必須要件](#-必須要件-prerequisites)
  - [KoboldCpp で 📕 ST Memory Books を使用するためのヒント](#koboldcpp-で--st-memory-books-を使用するためのヒント)
  - [Llama.cpp で 📕 ST Memory Books を使用するためのヒント](#llamacpp-で--st-memory-books-を使用するためのヒント)
- [推奨されるグローバル World Info/ロアブック有効化設定](#-推奨されるグローバル-world-infoロアブック有効化設定)
- [はじめに](#-はじめに)
  - [1. インストールと読み込み](#1-インストールと読み込み)
  - [2. シーンをマークする](#2-シーンをマークする)
  - [3. メモリを作成する](#3-メモリを作成する)
- [メモリの種類: シーン vs 要約](#-メモリの種類-シーン-vs-要約)
  - [シーンメモリ (デフォルト)](#-シーンメモリ-デフォルト)
  - [要約統合](#-要約統合)
- [メモリ生成](#-メモリ生成)
  - [JSON のみ出力](#json-のみ出力)
  - [組み込みプリセット](#組み込みプリセット)
  - [カスタムプロンプト](#カスタムプロンプト)
- [ロアブック統合](#-ロアブック統合)
- [メモリーブックにクリップ](#️-メモリーブックにクリップ)
- [トピッククリップ](#-トピッククリップ)
- [スラッシュコマンド](#-スラッシュコマンド)
- [グループチャットのサポート](#-グループチャットのサポート)
- [動作モード](#-動作モード)
  - [自動モード (デフォルト)](#自動モード-デフォルト)
  - [ロアブック自動作成モード](#ロアブック自動作成モード)
  - [手動ロアブックモード](#手動ロアブックモード)
- [トラッカーとサイドプロンプト](#-トラッカーとサイドプロンプト)
- [圧縮](#-圧縮)
- [高度なカスタマイズのための Regex 統合](#-高度なカスタマイズのための-regex-統合)
- [プロファイル管理](#-プロファイル管理)
- [設定と構成](#-設定と構成)
  - [グローバル設定](#グローバル設定)
  - [プロファイルフィールド](#プロファイルフィールド)
- [タイトルのフォーマット](#-タイトルのフォーマット)
- [コンテキストメモリ](#-コンテキストメモリ)
- [任意のジョブキュー](#optional-job-queue-chat-top-bar-required)
- [視覚的フィードバックとアクセシビリティ](#-視覚的フィードバックとアクセシビリティ)
- [FAQ](#faq)
  - [メモリ用に別のロアブックを作成すべきですか？それとも他の用途ですでに使用しているものと同じロアブックを使用できますか？](#メモリ用に別のロアブックを作成すべきですかそれとも他の用途ですでに使用しているものと同じロアブックを使用できますか)
  - [ベクトルを実行する必要がありますか？](#ベクトルを実行する必要がありますか)
  - [Memory Books が唯一のロアブックである場合、「Delay until recursion」を使用すべきですか？](#memory-books-が唯一のロアブックである場合delay-until-recursionを使用すべきですか)
  - [なぜ AI がエントリを見ていないのですか？](#なぜ-ai-がエントリを見ていないのですか)
- [トラブルシューティング](#troubleshooting)
- [Lorebook Ordering (STLO) でパワーアップ](#-lorebook-ordering-stlo-でパワーアップ)
- [文字ポリシー](#-文字ポリシー-v451)
- [開発者向け](#-開発者向け)
  - [拡張機能のビルド](#拡張機能のビルド)
  - [Git フック](#git-フック)

---

## 📋 必須要件 (Prerequisites)

- **SillyTavern:** 1.14.0 以上（最新版を推奨）
- **任意のジョブキュー:** STMB はジョブキューなしでも動作します。キュー機能を使うには、チャットウィンドウ上部にバーを追加する SillyTavern 公式拡張機能 **Chat Top Bar** / **Chat Top Info Bar** をインストールして有効化してください。STMB はそのバーを使って **メモリーブックのジョブ** ボタンとドロワーを表示します。
- **Chat Completion サポート:** OpenAI、Claude、Anthropic、OpenRouter、またはその他の Chat Completion API を完全にサポートしています。
- **Text Completion サポート:** Kobold、TextGen などの Text Completion API は、Chat Completion（OpenAI 互換）API エンドポイント経由で接続されている場合にサポートされます。下記の KoboldCpp のヒントに従って Chat Completion API 接続を設定することをお勧めします（Ollama やその他のソフトウェアを使用している場合は必要に応じて調整してください）。その後、STMB プロファイルを設定し、`Custom`（推奨）または完全な手動設定（Custom が失敗する場合、またはカスタム接続が複数ある場合のみ）を使用してください。

**注:** Text Completion を使用する場合は、Chat Completion プリセットが必須です。

### KoboldCpp で 📕 ST Memory Books を使用するためのヒント

ST で以下のように設定してください（STMB が動作することを確認した後で Text Completion に戻せます）。

- Chat Completion API
- Custom chat completion source
- エンドポイント: `http://localhost:5001/v1`（`127.0.0.1:5000/v1` も使用できます）
- `custom API key` には何かを入力してください（内容は何でも構いませんが、ST では必須です）。
- モデル ID は `koboldcpp/modelname` にする必要があります（モデル名に `.gguf` を入れないでください！）。
- Chat Completion プリセットをダウンロードしてインポートしてください（どれでも構いません）。Chat Completion プリセットが存在するだけで、「not supported」系のエラーを避けられます。
- Chat Completion プリセットの最大応答長を少なくとも 2048 に変更してください。4096 を推奨します（小さいと途中で切れるリスクがあります）。

### Llama.cpp で 📕 ST Memory Books を使用するためのヒント

Kobold と同様に、ST で _Chat Completion API_ として以下を設定してください（STMB が動作することを確認した後で元の設定に戻せます）。

- Chat Completion API 用の新しい接続プロファイルを作成します。
- Completion Source: `Custom (Open-AI Compatible)`
- Endpoint URL: Docker で ST を実行している場合は `http://host.docker.internal:8080/v1`、それ以外は `http://localhost:8080/v1`
- Custom API key: 何かを入力してください（ST では必須です）。
- Model ID: `llama2-7b-chat.gguf`（または使用しているモデル。llama.cpp で複数モデルを実行していない場合は何でも構いません）
- Prompt post-processing: none

Llama.cpp を起動する場合、起動を簡単にするために、以下のような内容をシェルスクリプトまたは bat ファイルに入れることをお勧めします。

```sh
llama-server -m <model-path> -c <context-size> --port 8080
```

## 💡 推奨されるグローバル World Info/ロアブック有効化設定

- **Match Whole Words:** チェックなし（false）のままにしてください。
- **Scan Depth:** 高いほど良いです（私の設定は 8 です）。
- **Max Recursion Steps:** 2（一般的な推奨値であり、必須ではありません）。
- **Context %:** 80%（100,000 トークンのコンテキストウィンドウに基づく）- チャット履歴やボットが極端に重くないことを前提としています。
- 追加の注意: メモリロアブックが唯一のロアブックである場合は、STMB プロファイルで `Delay until recursion` が無効になっていることを確認してください。有効だとメモリがトリガーされません。

---

## 🚀 はじめに

### 1. **インストールと読み込み**

![これらのボタンが表示されるのを待ちます](https://github.com/aikohanasaki/imagehost/blob/main/STMemoryBooks/startup.png)


- SillyTavern をロードし、キャラクターまたはグループチャットを選択します。
- チャットメッセージに山括弧ボタン（► ◄）が表示されるのを待ちます（最大 10 秒かかる場合があります）。


### 2. **シーンをマークする**

![クリックされた開始ボタン](https://github.com/aikohanasaki/imagehost/blob/main/STMemoryBooks/button-start.png)

![シーン中のボタン](https://github.com/aikohanasaki/imagehost/blob/main/STMemoryBooks/button-middle.png)

![クリックされた終了ボタン](https://github.com/aikohanasaki/imagehost/blob/main/STMemoryBooks/button-end.png)


- シーンの最初のメッセージで ► をクリックします。
- シーンの最後のメッセージで ◄ をクリックします。

以下は、山括弧ボタンをクリックしたときの表示例です。色は使用している CSS テーマによって異なる場合があります。


### 3. **メモリを作成する**

- 拡張機能メニュー（入力欄の左側にある魔法の杖 🪄）を開き、「Memory Books」をクリックするか、`/creatememory` スラッシュコマンドを使用します。
- プロンプトが表示された場合は、設定（プロファイル、コンテキスト、API/モデル）を確認します。
- AI による生成と自動ロアブックエントリ作成を待ちます。

---

## 🧩 メモリの種類: シーン vs 要約

📕 Memory Books は、**シーンメモリ** と **多段階の要約統合** をサポートしています。それぞれ、異なる種類の連続性を扱うために設計されています。

### 🎬 シーンメモリ (デフォルト)

シーンメモリは、特定のメッセージ範囲で **何が起こったか** を記録します。

- 明示的なシーン選択（► ◄）に基づきます。
- 瞬間ごとの回想に適しています。
- 会話、行動、直後の結果を保持します。
- 頻繁に使用するのに向いています。

これは標準的で、最も一般的に使用されるメモリタイプです。

---

### 🌈 要約統合

![統合ボタン](https://github.com/aikohanasaki/imagehost/blob/main/STMemoryBooks/button-consolidate.png)


要約統合は、複数のメモリまたは要約にまたがって **時間の経過とともに何が変わったか** を記録します。

単一シーンを要約するのではなく、統合要約は以下に焦点を当てます。

- キャラクターの成長と関係性の変化
- 長期的な目標、緊張、解決
- 感情の軌跡と物語の方向性
- 安定して残すべき永続的な状態変化

最初の統合階層は **Arc** で、シーンメモリから作られます。長く続く物語向けに、さらに上位の階層もサポートされています。

- Arc
- Chapter
- Book
- Legend
- Series
- Epic

> 💡 これはシーンログではなく、*これまでのあらすじ* と考えてください。

#### 統合要約を使うタイミング

- 大きな関係性の変化があった後
- 章やアークの終わり
- 動機、信頼、力関係が変化したとき
- 物語の新しいフェーズを始める前

#### 仕組み

- 統合要約は、生のチャット本文からではなく、既存の STMB メモリ/要約から生成されます。
- **Consolidate Memories** ツールでは、対象の要約階層と元になるエントリを選択できます。
- STMB は、選択した要約階層を監視し、その階層が保存済みの最小対象数に達したときに yes/later の確認を表示できます。
- より高レベルの要約に役割を引き継がせたい場合、統合後に元のエントリを無効化できます。
- AI の要約応答が失敗した場合は、UI で確認・修正してからコミットを再試行できます。

これにより、以下の効果があります。

- トークン使用量の削減
- 長いチャットでの物語の連続性向上

---

## 📝 メモリ生成

### **JSON のみ出力**

すべてのプロンプトとプリセットは、AI に有効な JSON のみを返すよう指示する **必要があります**。例：

```json
{
  "title": "短いシーンタイトル",
  "content": "シーンの詳細な要約...",
  "keywords": ["キーワード1", "キーワード2"]
}
```

**応答に他のテキストを含めることは許可されていません。**

### **組み込みプリセット**

1. **Summary:** 詳細なビートごとの要約。
2. **Summarize:** タイムライン、ビート、相互作用、結果のための Markdown ヘッダー。
3. **Synopsis:** 包括的で構造化された Markdown。
4. **Sum Up:** タイムライン付きの簡潔なビート要約。
5. **Minimal:** 1〜2 文の要約。
6. **Northgate:** 創作向けの文学的な要約スタイル。
7. **Aelemar:** プロット要点とキャラクターの記憶に重点を置きます。
8. **Comprehensive:** キーワード抽出を改善したシノプシス型要約。

### **カスタムプロンプト**

- 独自に作成できますが、上記のように有効な JSON を返す **必要があります**。

---

## 📚 ロアブック統合

- **自動エントリ作成:** 新しいメモリは、すべてのメタデータを含むエントリとして保存されます。
- **フラグベースの検出:** `stmemorybooks` フラグを持つエントリのみがメモリとして認識されます。
- **自動ナンバリング:** 複数の形式（`[000]`, `(000)`, `{000}`, `#000`）をサポートする、連番のゼロ埋めナンバリングです。
- **手動/自動順序:** プロファイルごとの挿入順序設定。
- **エディタの更新:** メモリを追加した後、必要に応じてロアブックエディタを自動更新します。

> **既存のメモリは変換が必要です！**
> [`Lorebook Converter`](../resources/lorebookconverter.html) を使用して、`stmemorybooks` フラグと必須フィールドを追加してください。

---

## ✂️ メモリーブックにクリップ

![テキストをクリップ](https://github.com/aikohanasaki/imagehost/blob/main/STMemoryBooks/clip.png)


「メモリーブックにクリップ」は、すばやい「これを覚えておいて」メモ用です。チャット内の重要なテキストを選択し、フローティングのはさみボタンをクリックすると、ロアブックエディタを先に開かずに、選択したテキストをメモリーブックの箇条書きとして保存できます。

継続的に更新されるトラッカーが必要なら、代わりにサイドプロンプトを使ってください。短く言うと: **クリップ = 保存した1つの事実、サイドプロンプト = 継続更新されるトラッカー。**

#### 仕組み

- 覚えておきたい正確なテキストを選択します。
- フローティングのはさみボタンをクリックします。このボタンは Memory Books ポップアップでオン/オフできます。
- 既存のクリップエントリを選ぶか、新しいエントリを作成します。
- 保存前に、現在のエントリと更新後のプレビューを確認します。
- 必要ならエントリ/セクション名を変更します。

クリップエントリは、エントリタイトルの末尾に `[STMB Clip]` が付いた通常のロアブックエントリです。例:

```txt
Seraphina Healed Me [STMB Clip]
```

エントリ内に表示されるセクションでは、`[STMB Clip]` を除いたタイトルを使います。

```md
=== Seraphina Healed Me ===

- Seraphina healed my wounds with magic.
- Seraphina, guardian of this forest

=== END Seraphina Healed Me ===
```

#### ヒント

- 1つのクリップエントリには1つのセクションがあります。`Things {{user}} Likes`、`Pet Names`、`Food Preferences` のように焦点のはっきりしたタイトルを使うと、キーワードを具体的に保ちやすくなります。
- 新しいクリップエントリは、常時有効またはキーワード起動にできます。常時有効が最も簡単です。エントリが時々だけ必要な場合は、キーワードの方が向いています。
- 既存のエントリは、タイトル末尾に `[STMB Clip]` を追加することでクリップエントリにできます。
- 長くなったクリップエントリでは、確認または圧縮を促すリマインダーが表示されることがあります。圧縮は、Clip、Side Prompt、STMB メモリエントリを、元の内容と置き換える前によりトークン効率よくするのに役立ちます。
- クリップエントリには出典情報は追加されません。保存されるのは、クリップするために選んだテキストだけです。

---

## 🔎 トピッククリップ

トピッククリップは、1つのトピックについて焦点を絞ったクリップ形式のメモリエントリを作成または更新します。

すでにSTMBメモリが保存されていて、そのメモリから関連する詳細を集めた「このトピックについて」の整理済みエントリが欲しいときに使います。例：

- `Seraphinaについて`
- `{{user}}の魔法について`
- `AlexとMiraの関係について`
- `Black Harborの調査について`

トピッククリップは通常の「メモリーブックにクリップ」とは異なります。通常のクリップは、選択したチャット本文をそのまま保存します。トピッククリップは既存のSTMBメモリエントリを読み、指定したトピックに関する詳細をAIに抽出させ、保存前に編集可能なドラフトを表示します。

#### 仕組み

1. Memory Booksを開きます。
2. **🔎 トピッククリップ**をクリックします。
3. **ソースメモリーブック**を選びます。
4. **トピック**を入力します。
5. 有効化用の**キーワード**を入力します。空欄ならトピックが使われます。
6. 新しいトピッククリップを作成するか、既存の`[STMB Clip]`エントリを更新するかを選びます。
7. **生成プロファイル**を選びます。
8. **ドラフトを生成**をクリックします。
9. ドラフトを確認し、必要なら編集します。
10. 内容に問題がなければ**トピッククリップを保存**をクリックします。

トピッククリップは、通常のクリップエントリとして`[STMB Clip]`付きで保存されます。新しいエントリのタイトルは次のようになります：

```txt
Seraphinaについて [STMB Clip]
```

#### 既存のトピッククリップを更新する

既存のトピッククリップを更新すると、STMBは前回の成功した実行で使われたソースメモリを記録します。次回の更新では、通常は新規または変更されたソースメモリだけを使います。

対象となるすべてのメモリからエントリ全体を作り直したい場合は、ドラフト生成前に**すべてのソースメモリから再構築**を有効にします。

#### 注意

- トピッククリップは、確認済みのSTMBメモリエントリのみをソースとして使います。
- クリップエントリとサイドプロンプトエントリはソースメモリとして使われません。
- 更新対象は既存の`[STMB Clip]`エントリです。
- AIドラフトは保存前に必ず確認・編集できます。
- **トピッククリップを保存**をクリックするまで、STMBは生成されたドラフトを保存しません。
- リクエストが大きい場合、実行前にSTMBがトークン警告を表示することがあります。

---

## 🆕 スラッシュコマンド

- `/creatememory` - マーク済みシーンからメモリを作成します。
- `/scenememory X-Y` - シーン範囲を設定してメモリを作成します（例: `/scenememory 10-15`）。
- `/nextmemory` - 前回のメモリの終端から現在のメッセージまでを使ってメモリを作成します。
- `/stmb-catchup interval=x start=y end=y` - 既存の長いチャットを、指定したメッセージ範囲ごとに interval サイズのチャンクへ分割して、キャッチアップ用メモリを作成します。
- `/sideprompt "Name" {{macro}}="value" [X-Y]` - サイドプロンプトを実行します（`{{macro}}` は任意です）。
- `/sideprompt-set "Set Name" [X-Y]` - 保存済みの Side Prompt Set を実行します。
- `/sideprompt-macroset "Set Name" {{macro}}="value" [X-Y]` - Side Prompt Set を実行し、再利用可能なマクロ値を渡します。
- `/sideprompt-on "Name" | all` - 名前指定または `all` で Side Prompt を有効化します。
- `/sideprompt-off "Name" | all` - 名前指定または `all` で Side Prompt を無効化します。
- `/stmb-highest` - このチャットで処理済みメモリの最も高い message id を返します。
- `/stmb-set-highest <N|none>` - このチャットの処理済み最高 message id を手動で設定します。
- `/stmb-stop` - 進行中のすべての STMB 生成を全体で停止します（緊急停止）。

### `/stmb-catchup`

既存の長いチャットを STMB メモリに変換したいときに `/stmb-catchup` を使用します。構文: `/stmb-catchup interval=x start=y end=y`

例: `/stmb-catchup interval=30 start=0 end=300`

---

## 👥 グループチャットのサポート

STMB は、1 対 1 のチャットで使用するものと同じ手動、自動、スラッシュコマンドのメモリーツールをグループチャットでも利用できます。グループチャット専用モードを有効にする必要はありません。グループを選択し、通常どおり STMB を使用してください。

#### 参加者を認識するメモリー

- STMB は各グループチャットメッセージに割り当てられた話者を読み取り、生成される要約内で誰の発言・行動かを明確に保ちます。
- 参加したグループメンバーを特定できる場合、保存されるメモリーには、そのメンバーを含む SillyTavern のキャラクターフィルターが設定されます。これにより、グループメモリーは実際にシーンへ参加したキャラクターに結び付けられます。
- シーンマーカー、処理済みの最大メッセージ ID、手動ロアブックの選択、その他のチャット別状態は、アクティブなグループチャットに保存されます。チャットを切り替えても、それらの設定が別のチャットへ移動することはありません。

#### 自動モードと自動作成モード：1 つの Memory Book

自動モードは、グループチャットに紐付けられたロアブックを使用します。自動作成モードは、グループにまだロアブックがない場合に 1 つ作成して紐付けることができます。どちらのモードでも、メモリーはそのグループの Memory Book に保存され、話者を特定できる場合は参加者フィルターが自動的に追加されます。

これは最も簡単な設定で、ほとんどのグループチャットにはこれで十分です。

#### 手動モード：グループ用とキャラクター用の Memory Book

手動ロアブックモードでは、メインのグループ Memory Book に加えて、各グループメンバー用に指定した Memory Book を管理できます。キャラクターごとのロアブック指定には、[SillyTavern-LorebookOrdering (STLO)](https://github.com/aikohanasaki/SillyTavern-LorebookOrdering) のインストールと有効化が必要です。

1. グループチャットを開き、**手動ロアブックモード**を有効にします。
2. メインの手動ロアブックを選択します。これが基準となるグループ Memory Book になります。
3. **グループキャラクターのロアブック**で、各メンバーのロアブックを選択します。同じロアブックを複数のキャラクターに割り当てることもできます。
4. 通常どおりメモリーを作成します。
5. 参加したキャラクターを確認します。STMB はメッセージから検出したメンバーをあらかじめ選択します。誰も選択しない場合、メモリーはすべてのグループメンバーに適用されます。

STMB は基準となるメモリーをグループ Memory Book に保存し、選択した参加者に指定された Memory Book へコピーします。関連するエントリーは内部でリンクされるため、グループ統合とキャラクター統合のタイムラインを揃えて維持できます。必要なキャラクターロアブックが存在しない、または削除されている場合、STMB は一部だけのメモリーを残さず処理を停止します。

参加者の確認には **今後、検出された参加者を自動的に承認する** オプションがあります。話者検出を信頼し、メモリーごとに一覧を承認したくない場合に有効にしてください。

#### グループ用とキャラクター用の個別プロンプト（任意）

同じメモリーの異なるバージョンを作成するには：

1. **プロファイルマネージャー**を開き、メモリー作成に使用するプロファイルを編集します。
2. **グループチャットでグループ用とキャラクター用の個別プロンプトを使用する**を有効にします。
3. **グループ要約プロンプト**と**キャラクター要約プロンプト**を選択します。

グループプロンプトは、メインのグループ Memory Book に共有版を書き込みます。STLO を使用する手動モードでは、キャラクタープロンプトによって、個別に割り当てられたキャラクター Memory Book に、そのキャラクターに焦点を当てた版を作成できます。追加の生成リクエストが必要になりますが、共有メモリーにはシーン全体を記述し、個別コピーにはそのキャラクターにとって重要だったことを集中して記述できます。複数のメンバーが同じ割り当て先 Memory Book を共有している場合、STMB はそこに 1 つの共有コピーだけを保持します。

> 💡 **推奨：** まずはグループ用 Memory Book を 1 つだけ使用してください。個々のメンバーに異なる知識、連続性、コンテキストが必要になった場合のみ、キャラクター別 Memory Book を追加してください。

---

## 🧭 動作モード

### **自動モード (デフォルト)**

![チャットロアブックのバインド例](https://github.com/aikohanasaki/imagehost/blob/main/STMemoryBooks/chatlorebook.png)


- **動作:** 現在のチャットにバインドされているロアブックを自動的に使用します。
- **推奨:** シンプルさとスピード重視の場合。ほとんどのユーザーはここから始めるべきです。
- **使用法:** キャラクターまたはグループチャットの `Chat Lorebooks` ドロップダウンでロアブックが選択されていることを確認してください。


### **ロアブック自動作成モード**

- **動作:** ロアブックが存在しない場合、カスタム命名テンプレートを使用して新しいロアブックを自動的に作成し、バインドします。
- **推奨:** 新規ユーザーや素早いセットアップに適しています。ワンクリックでのロアブック作成に向いています。
- **使用法:**
  1. 拡張機能の設定で `Auto-create lorebook if none exists` を有効にします。
  2. 命名テンプレートを設定します（デフォルト: `LTM - {{char}} - {{chat}}`）。
  3. バインドされたロアブックがない状態でメモリを作成すると、ロアブックが自動的に作成・バインドされます。
- **テンプレートプレースホルダー:** `{{char}}`（キャラクター名）、`{{user}}`（あなたの名前）、`{{chat}}`（チャット ID）
- **スマートナンバリング:** 重複する名前が存在する場合、自動的に番号（2, 3, 4...）を追加します。
- **注:** 手動ロアブックモードとは同時に使用できません。

### **手動ロアブックモード**

- **動作:** メインのチャットバインドロアブックを無視して、チャットごとにメモリ用の別ロアブックを選択できます。
- **推奨:** メモリを特定の別ロアブックに保存したい上級ユーザー向けです。
- **使用法:**
  1. 拡張機能の設定で `Enable Manual Lorebook Mode` を有効にします。
  2. チャットで初めてメモリを作成するときに、ロアブックを選択するよう求められます。
  3. この選択は、クリアするか自動モードに戻すまで、そのチャットに保存されます。
- **注:** ロアブック自動作成モードとは同時に使用できません。

---

### 🎡 トラッカーとサイドプロンプト

![トラッカーとサイドプロンプトの場所](https://github.com/aikohanasaki/imagehost/blob/main/STMemoryBooks/sp.png)


> 📘 サイドプロンプトには専用ガイドがあります: [Side Prompts Guide](side-prompts-jp.md)。セット、マクロ、例、トラブルシューティングはこちらを参照してください。
> 🎡 正確なクリック手順が必要な場合は、[Side Prompts を有効化する Scribe 手順](https://scribehow.com/viewer/How_to_Enable_Side_Prompts_in_Memory_Books__fif494uSSjCmxE2ZCmRGxQ) を参照してください。

サイドプロンプトは、進行中のチャット状態を維持するための独立した STMB プロンプト実行です。通常のキャラクター返信を肥大化させたくないトラッカーや補助メモに使用できます。例：

- 💰 インベントリとリソース（「ユーザーはどのアイテムを持っているか？」）
- ❤️ 関係ステータス（「X は Y についてどう感じているか？」）
- 📊 キャラクター統計（「現在の体力、スキル、評判」）
- 🎯 クエスト進行状況（「どの目標がアクティブか？」）
- 🌍 世界の状態（「設定で何が変わったか？」）

#### **アクセス:** Memory Books 設定から「🎡 Trackers & Side Prompts」をクリックします。

#### **機能:**

- サイドプロンプトを表示、作成、複製、編集、削除、エクスポート、インポートできます。
- サイドプロンプトを手動、メモリ作成後、または Side Prompt Set の一部として実行できます。
- `{{user}}` や `{{char}}` のような標準 SillyTavern マクロを使用できます。
- 実行時に値が必要な場合は、`{{npc name}}` のようなランタイムマクロを使用できます。
- サイドプロンプトの出力を、メモリロアブック内の個別のサイドプロンプトエントリとして保存できます。

#### **使用のヒント:**

- 新しいプロンプトを作成する場合は、組み込みテンプレートからコピーしてください。
- サイドプロンプトは JSON を返す必要はありません。プレーンテキストや Markdown で問題ありません。
- サイドプロンプトは通常、更新/上書きされます。メモリは順番に保存されます。
- 手動構文は `/sideprompt "Name" {{macro}}="value" [X-Y]` です。
- チャットに順序付きのトラッカー一式が必要な場合は、Side Prompt Set を使用してください。
- メモリ作成後用の Side Prompt Set を選択すると、そのチャットでは個別に有効化されたメモリ作成後のサイドプロンプトが置き換えられます。
- 追加のサイドプロンプトテンプレートライブラリは [JSON ファイル](../resources/SidePromptTemplateLibrary.json) として用意されています。インポートするだけで使用できます。

---

## 🧹 圧縮

![圧縮メニューを開く場所](https://github.com/aikohanasaki/imagehost/blob/main/STMemoryBooks/compaction.png)


圧縮は、STMB が管理するロアブックエントリをよりトークン効率のよい形に整理するための確認ワークフローです。AI に既存のエントリを 1 件だけ書き直させ、元の内容と圧縮ドラフトを並べて表示してから、置き換えるかどうかを選べます。

メインの Memory Books ポップアップから **📝 圧縮** を開けます。長いクリップエントリでは、クリップ操作中に **エントリを圧縮** ボタンが表示される場合もあります。

#### 対象になるエントリ

圧縮では、選択したメモリーブック内の対象エントリが表示されます。

- `[STMB Clip]` が付いたクリップエントリ
- サイドプロンプトエントリ
- Memory Books によってフラグ付けされた STMB メモリエントリ

STMB が管理していない通常のロアブックエントリは表示されません。

#### 使い方

1. Memory Books を開き、**📝 圧縮** をクリックします。
2. **メモリーブック** を選択します。現在のチャットに有効なメモリーブックがある場合、STMB がそれを初期選択します。ない場合は、検索可能なドロップダウンから選びます。
3. **圧縮プロファイル** を選択します。これは圧縮リクエストに使う AI 接続/モデルを決めます。
4. 必要に応じて、**圧縮プロンプトを編集** をクリックし、AI に送る指示を調整します。
5. 書き直したいエントリの横にある **エントリを圧縮** をクリックします。
6. **元の内容** と **圧縮ドラフト** を比較します。STMB は両方の推定トークン数を表示します。
7. 必要ならドラフトを編集し、**圧縮版に置き換え**、**圧縮ドラフトをコピー**、または **キャンセル** を選びます。

STMB が自動で元のエントリを置き換えることはありません。ロアブックエントリが変更されるのは、**圧縮版に置き換え** をクリックした場合だけです。

#### 圧縮プロンプト

圧縮プロンプトは編集できます。デフォルトのプロンプトは、重要な事実、名前、代名詞、マクロ、ラッパー見出し、終了マーカーを維持しながら、重複や価値の低い表現を削るよう AI に指示します。

対応するプロンプトプレースホルダー:

- `{{ENTRY_CONTENT}}` — 現在のロアブックエントリ内容。このプレースホルダーは必須です。
- `{{ENTRY_KIND}}` — Clip、SidePrompt、Memory などのエントリ種別。
- `{{ENTRY_TITLE}}` — ロアブックエントリのタイトル。

組み込みの圧縮プロンプトに戻したい場合は、プロンプトエディタで **デフォルトにリセット** を使用してください。

#### 向いている用途

- 長くなったクリップエントリ
- 反復した記録がたまったサイドプロンプトのトラッカーエントリ
- 正しいが冗長になった STMB メモリエントリ
- 常時有効で、コンテキストを消費しすぎているエントリ

#### 向いていない用途

- 新しい事実を追加する
- 生のチャットを要約する
- 新しいメモリを作成する
- STMB が管理していない通常のロアブックエントリを書き換える

---

## 🧠 高度なカスタマイズのための Regex 統合

![Regex を設定](https://github.com/aikohanasaki/imagehost/blob/main/STMemoryBooks/regex.png)


- **テキスト処理の完全制御:** Memory Books は SillyTavern の **Regex** 拡張機能と統合され、次の 2 つの重要な段階で強力なテキスト変換を適用できます。
  1. **プロンプト生成:** **User Input** 配置を対象にした regex スクリプトを作成することで、AI に送信されるプロンプトを自動的に変更します。
  2. **応答解析:** **AI Output** 配置を対象にすることで、保存前に AI の生応答をクリーンアップ、再フォーマット、標準化します。
- **複数選択のサポート:** 送信前処理と受信後処理のそれぞれについて、複数のスクリプトを選択できます。
- **仕組み:** STMB で `Use regex (advanced)` を有効にし、`📐 Configure regex…` をクリックして、AI への送信前および解析/保存前に実行するスクリプトを選択します。
- **重要:** Regex の選択は STMB 側で制御されます。ここで選択されたスクリプトは、Regex 拡張機能側で現在無効になっていても **実行されます**。

---

## 👤 プロファイル管理

![プロファイル管理](https://github.com/aikohanasaki/imagehost/blob/main/STMemoryBooks/profiles.png)


- **プロファイル:** 各プロファイルには、API、モデル、温度、プロンプト/プリセット、タイトル形式、およびロアブック設定が含まれます。
- **インポート/エクスポート:** プロファイルを JSON として共有できます。
- **プロファイル作成:** 詳細オプションポップアップを使用して、新しいプロファイルを保存します。
- **プロファイルごとのオーバーライド:** メモリ作成のために API/モデル/温度を一時的に切り替え、その後元の設定を復元します。
- **組み込み Provider/Profile:** STMB には、アクティブな SillyTavern 接続/設定を直接使用する必須の `Current SillyTavern Settings` オプションが含まれています。

---

## ⚙️ 設定と構成

![メイン設定パネル 1](https://github.com/aikohanasaki/imagehost/blob/main/STMemoryBooks/profile1.png)
![メイン設定パネル 2](https://github.com/aikohanasaki/imagehost/blob/main/STMemoryBooks/profile2.png)
![メイン設定パネル 3](https://github.com/aikohanasaki/imagehost/blob/main/STMemoryBooks/profile3.png)


### **グローバル設定**

[Youtube での短い概要動画](https://youtu.be/mG2eRH_EhHs)

- **Manual Lorebook Mode:** チャットごとにロアブックを選択できるようにします。
- **Auto-create lorebook if none exists:** ⭐ *v4.2.0 の新機能* - 命名テンプレートを使用してロアブックを自動作成し、バインドします。
- **Lorebook Name Template:** ⭐ *v4.2.0 の新機能* - `{{char}}`、`{{user}}`、`{{chat}}` プレースホルダーを使用して、自動作成されるロアブック名をカスタマイズします。
- **Allow Scene Overlap:** 重複するメモリ範囲を許可または防止します。
- **Always Use Default Profile:** 確認ポップアップをスキップします。
- **Show memory previews:** ロアブックに追加する前にメモリを確認・編集するためのプレビューポップアップを有効にします。
- **Show Notifications:** トーストメッセージのオン/オフを切り替えます。
- **Refresh Editor:** メモリ作成後にロアブックエディタを自動更新します。
- **Max Response Tokens:** メモリ要約の最大生成長を設定します。
- **Token Warning Threshold:** 大きなシーンに対する警告レベルを設定します。
- **Default Previous Memories:** コンテキストとして含める以前のメモリ数（0〜7）。
- **Auto-create memory summaries:** 一定間隔での自動メモリ作成を有効にします。
- **Auto-Summary Interval:** メモリ要約を自動作成するまでのメッセージ数。
- **Auto-Summary Buffer:** 自動要約を設定したメッセージ数だけ遅らせます。
- **Prompt for consolidation when a tier is ready:** 選択した要約階層に十分な対象元エントリがある場合、yes/later の確認を表示します。
- **Auto-Consolidation Tiers:** 準備完了時に確認を表示する要約階層を 1 つ以上選択します。現在は Arc から Series まで対応しています。
- **Unhide hidden messages before memory generation:** メモリ作成前に `/unhide X-Y` を実行できます。
- **Auto-hide messages after adding memory:** 処理済みメッセージ全体、または直近のメモリ範囲のみを自動非表示にできます。
- **Use regex (advanced):** 送信前/受信後処理用の STMB regex 選択ポップアップを有効にします。
- **Memory Title Format:** 選択またはカスタマイズします（下記参照）。


### **プロファイルフィールド**

![プロファイル設定](https://github.com/aikohanasaki/imagehost/blob/main/STMemoryBooks/Profile.png)


- **Name:** 表示名。
- **API/Provider:** `Current SillyTavern Settings`、openai、claude、custom、full manual、およびその他の対応プロバイダー。
- **Model:** モデル名（例: gpt-4、claude-3-opus）。
- **Temperature:** 0.0〜2.0。
- **Prompt or Preset:** カスタムまたは組み込み。
- **Title Format:** プロファイルごとのテンプレート。
- **Activation Mode:** Vectorized、Constant、Normal。
- **Position:** ↑Char、↓Char、↑EM、↓EM、↑AN、↓AN、Outlet（およびフィールド名）。
- **Order Mode:** Auto/manual。
- **Recursion:** 再帰を防止、または再帰まで遅延。

---

## 🏷️ タイトルのフォーマット

![タイトル形式](https://github.com/aikohanasaki/imagehost/blob/main/STMemoryBooks/titleformat.png)
![タイトル形式の一覧](https://github.com/aikohanasaki/imagehost/blob/main/STMemoryBooks/titleformats.png)


強力なテンプレートシステムを使用して、ロアブックエントリのタイトルをカスタマイズできます。

- **プレースホルダー:**
  - `{{title}}` - AI によって生成されたタイトル（例: `運命の出会い`）。
  - `{{scene}}` - メッセージ範囲（例: `Scene 15-23`）。
  - `{{char}}` - キャラクターの名前。
  - `{{user}}` - ユーザー名。
  - `{{messages}}` - シーン内のメッセージ数。
  - `{{profile}}` - 生成に使用されたプロファイルの名前。
  - 現在の日付/時刻プレースホルダーをさまざまな形式で使用できます（例: 日付は `August 13, 2025`、時刻は `11:08 PM`）。
- **自動ナンバリング:** `[0]`、`[00]`、`(0)`、`{0}`、`#0` に加えて、`#[000]`、`([000])`、`{[000]}` のようなラップ形式も、連番のゼロ埋めナンバリングに使用できます。
- **カスタムフォーマット:** 独自のフォーマットを作成できます。v4.5.1 以降、絵文字、CJK、アクセント付き文字、記号などを含む、印刷可能なすべての Unicode 文字がタイトルで使用可能です。Unicode 制御文字のみがブロックされます。

---

## 🧵 コンテキストメモリ

![コンテキスト付きメモリ生成](https://github.com/aikohanasaki/imagehost/blob/main/STMemoryBooks/context.png)


- **最大 7 つの過去メモリ** をコンテキストとして含めることで、連続性を高められます。
- **トークン見積もり** には、正確性のためにコンテキストメモリが含まれます。
- **詳細オプション** により、1 回のメモリ実行だけプロンプト/プロファイル動作を一時的に上書きできます。


---

<a id="optional-job-queue-chat-top-bar-required"></a>
## 🧾 任意のジョブキュー（Chat Top Bar が必要）

![ST Memory Books ジョブキュー](https://github.com/aikohanasaki/imagehost/blob/main/STMemoryBooks/queue.png)


ジョブキューは任意ですが、強力な機能です。Memory Books を使うために必須ではありません。

**Chat Top Bar** / **Chat Top Info Bar** をインストールして有効化すると、STMB はチャット上部バーに **メモリーブックのジョブ** ボタンを追加します。このボタンを開くと、実行中、完了、失敗、キャンセル済み、またはレビューが必要な Memory Books ジョブを確認できるキュードロワーが表示されます。

特に次のような場合に便利です。

- 長めのシーンからメモリを作成する
- 統合を実行する
- メモリ作成後に Side Prompts を実行する
- 長いチャットで、進行状況やレビュー処理をより分かりやすくしたい

キューでは、ジョブの状態を表示したり、実行中のジョブをキャンセルしたり、失敗したジョブを再試行したり、完了したジョブを閉じたりできます。キュー内のジョブにユーザーレビューが必要な場合、STMB は安全でない上書きを黙って行う代わりに、そのジョブを **レビューが必要** として表示できます。

Chat Top Bar がインストールまたは有効化されていない場合でも、STMB は通常どおり動作します。ただし、ジョブキュー UI は利用できません。


### Chat Top Bar のインストール

![Chat Top Bar のインストール方法](https://github.com/aikohanasaki/imagehost/blob/main/STMemoryBooks/install.png)

---
## 🎨 視覚的フィードバックとアクセシビリティ

![すべての視覚状態を含むシーン選択例](https://github.com/aikohanasaki/imagehost/blob/main/STMemoryBooks/example.png)


- **ボタンの状態:**
  - 非アクティブ、アクティブ、有効な選択、シーン内、処理中。


- **アクセシビリティ:**
  - キーボードナビゲーション、フォーカスインジケーター、ARIA 属性、reduced motion、モバイル対応。

---

# FAQ

### メモリ用に別のロアブックを作成すべきですか？それとも他の用途ですでに使用しているものと同じロアブックを使用できますか？

メモリ用ロアブックは別のブックにすることをお勧めします。他のエントリと比べてメモリを整理しやすくなります。たとえば、グループチャットへ追加する、別のチャットで使用する、または STLO を使って個別のロアブック予算を設定する場合に便利です。

### ベクトルを実行する必要がありますか？

使用しても構いませんが、必須ではありません。Vectors 拡張機能を使用していない場合（私は使用していません）、キーワード経由で動作します。これは自動化されているため、どのキーワードを使うかを考える必要はありません。

### Memory Books が唯一のロアブックである場合、「Delay until recursion」を使用すべきですか？

いいえ。他の World Info やロアブックがない場合、`Delay until recursion` を選択すると最初のループがトリガーされず、何もアクティブにならない可能性があります。Memory Books が唯一のロアブックである場合は、`Delay until recursion` を無効にするか、少なくとも 1 つの追加 World Info/ロアブックを設定してください。

### なぜ AI がエントリを見ていないのですか？

まず、エントリが実際に送信されているか確認する必要があります。私はその確認に [WorldInfo-Info](https://github.com/aikohanasaki/SillyTavern-WorldInfoInfo) を使うのが好きです。

エントリがトリガーされ、AI に送信されている場合は、OOC で AI に強めに指摘するとよいかもしれません。例: `[OOC: WHY are you not using the information you were given? Specifically: (whatever it was)]` 😁

---

# Troubleshooting

- **拡張機能メニューに Memory Books が見つかりません！**
  設定は拡張機能メニュー（入力欄の左側にある魔法の杖 🪄）にあります。「Memory Books」を探してください。

![STMB 設定の場所](https://github.com/aikohanasaki/imagehost/blob/main/STMemoryBooks/menu.png)


- **ロアブックが利用できないか、選択されていない:**
  - 手動モードの場合、プロンプトが表示されたらロアブックを選択してください。
  - 自動モードの場合、ロアブックをチャットにバインドしてください。
  - または、自動作成のために `Auto-create lorebook if none exists` を有効にしてください。

- **Lorebook Validation Error:**
  - 以前バインドされていたロアブックを削除した可能性があります。新しいロアブックをバインドしてください（空でも構いません）。

- **シーンが選択されていない:**
  - 開始（►）ポイントと終了（◄）ポイントの両方をマークしてください。

- **シーンが既存のメモリと重複している:**
  - 別の範囲を選択するか、設定で `Allow Scene Overlap` を有効にしてください。


![シーン重複警告](https://github.com/aikohanasaki/imagehost/blob/main/STMemoryBooks/overlap.png)
![シーン重複を有効化](https://github.com/aikohanasaki/imagehost/blob/main/STMemoryBooks/overlap2.png)

- **AI が有効なメモリの生成に失敗した:**
  - JSON 出力をサポートするモデルを使用してください。
  - プロンプトとモデル設定を確認してください。

- **Token warning threshold を超えた:**
  - より小さなシーンを使用するか、しきい値を上げてください。

- **山括弧ボタンがない:**
  - 拡張機能が読み込まれるのを待つか、更新してください。

- **キャラクターデータが利用できない:**
  - チャット/グループが完全に読み込まれるのを待ってください。

---

## 📚 Lorebook Ordering (STLO) でパワーアップ

高度なメモリ整理と、より深いストーリー統合のために、STMB を [SillyTavern-LorebookOrdering (STLO)](https://github.com/aikohanasaki/SillyTavern-LorebookOrdering/blob/main/guides/STMB%20and%20STLO%20-%20Japanese.md) と併用することを強くお勧めします。ベストプラクティス、セットアップ手順、ヒントについてはガイドをご覧ください。

---

## 📝 文字ポリシー (v4.5.1+)

- **タイトルで許可:** アクセント付き文字、絵文字、CJK、記号などを含む、印刷可能なすべての Unicode 文字が許可されています。
- **ブロック:** Unicode 制御文字（U+0000–U+001F, U+007F–U+009F）のみがブロックされ、自動的に削除されます。

例と移行に関する注意点については、[文字ポリシーの詳細](../charset.md) を参照してください。

---

## 👨‍💻 開発者向け

### 拡張機能のビルド

この拡張機能はビルドに Bun を使用します。ビルド処理では、ソースファイルを minify して bundle します。

```sh
# 拡張機能をビルド
bun run build
```

### Git フック

このプロジェクトには、拡張機能を自動的にビルドし、ビルド成果物をコミットに含める pre-commit フックが含まれています。これにより、ビルド済みファイルが常にソースコードと同期されます。

**Git フックをインストールするには:**

```sh
bun run install-hooks
```

このフックは次の処理を行います。

- 各コミット前に `bun run build` を実行します。
- ビルド成果物をコミットに追加します。
- ビルドが失敗した場合、コミットを中止します。

---

*VS Code/Cline、広範なテスト、そしてコミュニティからのフィードバックにより、愛を込めて開発されました。* 🤖💕
